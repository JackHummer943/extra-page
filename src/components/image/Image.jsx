import './image.css';

const Image = (props) => {
  return <section className={props.className}></section>;
  {
    /*
    <section className={props.sectionClassName}>
      {/* <img className={props.imgClassName} alt={props.alt} /> 
      <div className="background_image"></div>
    </section>
  */
  }
};

export default Image;
